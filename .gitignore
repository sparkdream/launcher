node_modules/
dist/
.next/
out/
next-env.d.ts
*.tsbuildinfo
*.log

# launcher runtime state — never commit
*.sqlite
*.sqlite-*
data/
.env
.env.*

# secrets that must never land in git
*.pem
*.key
*key.json
launch.local.yaml

# machine-local Claude context (absolute paths)
CLAUDE.md
.claude/
